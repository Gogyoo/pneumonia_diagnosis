# "local" is to be used is operations are being done on one's own system.
# "gcp" corresponds to the Buckets service of GCP.
# "gce" corresponds to the Google Compute Engine service of GCP.
# /!\ remember to direnv reload

DATA_SOURCE = local # alternate: gcp
FITTING_TARGET = local # alternate: gce
MODEL_TARGET = local # alternate: gcp
